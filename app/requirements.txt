# Same as repo root — Cloud prefers deps next to the entrypoint file.
# Pin Streamlit <1.41 so streamlit-drawable-canvas can show TEM backgrounds
# (1.41+ broke image_to_url; data-URL workarounds stay blank on Cloud).
streamlit>=1.39,<1.41
plotly>=5.24
streamlit-image-coordinates>=0.1.6
streamlit-drawable-canvas>=0.9.3
numpy>=1.26
scipy>=1.11
pandas>=2.1
matplotlib>=3.8
Pillow>=10.4
scikit-image>=0.22
typer>=0.12
rich>=13.9
