# =============================================================================
# TEM-analysis — Streamlit Cloud + general install deps
# Cloud uses this file automatically. Pin Python via runtime.txt (3.11).
# For exact local Mac pins, see requirements-pinned.txt
# =============================================================================

# Web UI
streamlit>=1.39,<1.42
plotly>=5.24,<6

# Core scientific stack (wheels available on Python 3.11)
numpy>=1.26,<2.1
scipy>=1.11,<1.15
pandas>=2.1,<2.3
matplotlib>=3.8,<3.10

# Image processing
Pillow>=10.4,<12
scikit-image>=0.22,<0.25

# CLI helpers (imported by package)
typer>=0.12
rich>=13.9
