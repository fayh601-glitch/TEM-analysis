# =============================================================================
# TEM-analysis — CdSe/CdS nanorod length measurement from TEM images
# Python 3.10+ recommended
#
# Install:
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Optional system dependency for scale-bar OCR (pytesseract):
#   macOS:   brew install tesseract
#   Ubuntu:  sudo apt install tesseract-ocr
# =============================================================================

# --- Core scientific computing ---
numpy==2.2.6
scipy==1.15.3
pandas==2.3.0
matplotlib==3.10.3

# --- Image processing and I/O ---
Pillow==11.2.1
scikit-image==0.25.2
opencv-python-headless==4.11.0.86
imageio==2.37.0
tifffile==2025.5.10

# --- CLI & configuration ---
typer==0.16.0
rich==14.0.0
pydantic==2.11.5
pydantic-settings==2.9.1
python-dotenv==1.1.0

# --- Upload UI (optional; run with: streamlit run app/streamlit_app.py) ---
streamlit==1.45.1

# --- Scale bar OCR (future phase; requires system tesseract) ---
pytesseract==0.3.13

# --- Export & reporting ---
openpyxl==3.1.5

# --- Testing ---
pytest==8.4.0
pytest-cov==6.1.1

# --- Development & linting ---
ruff==0.11.12

# =============================================================================
# Future phases (uncomment when needed — not required for v1 classical CV MVP)
# =============================================================================

# Deep learning segmentation (Cellpose — good for low-contrast particles)
# cellpose==4.0.6
# torch>=2.0.0

# YOLO + SAM style pipelines (supported metal / STEM nanoparticle workflows)
# ultralytics==8.3.152
# segment-anything==1.0

# Microscopy vendor formats (Gatan DM3/DM4, etc.)
# ncempy==1.13.1
# hyperspy==2.3.0

# UV–Vis correlation & spectroscopy (Phase 2 — CdSe/CdS sizing curves, plotting)
# lmfit==1.3.3
